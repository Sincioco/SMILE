AI Instructions
--------------
Design and Coding Methodology:
	KISS (Keep It Simple Stupid) - The simpler the better.  Avoid over-engineering.  Avoid unnecessary complexity.  Avoid unnecessary features.  Avoid unnecessary bells and whistles.  Avoid unnecessary abstractions.  Avoid unnecessary patterns.  Avoid unnecessary frameworks.  Avoid unnecessary libraries.  Avoid unnecessary dependencies.  Avoid unnecessary code.  Avoid unnecessary files.  Avoid unnecessary folders.  Avoid unnecessary classes.  Avoid unnecessary methods.  Avoid unnecessary variables.

	KISS v2 (Keep It Speedy Sin) "The Sin Way" - Follow the original KISS method but with the mindset of creating a solution that gives the best possible performance both in terms of user-experience (first priority) and functional performance (second priority).  The user-experience may take precedence over the functional performance for any task.  For example, if perform a long-running task such as recalculating the positions of objects on screen (think Diagram 1), it should not lock the thread the user is on and give the impression that the application is stuck or sluggish.  The actual work, the functional part that does the calculations and moving objects on the screen, may be done slower, in chunks, or in a background / unobtrusive way, in order to give the user the best user-experience and responsiveness.  Never sacrifice performance for simplicity.  Speed is King!  But always design solutions and write code, that respect the user's experience and the application's responsiveness, over pure performance that often results in a negative user experience in the end.  If the two directions are in conflict, I will make it simple:  pick the user-experience first and just display a gauge to perform long running tasks in the background, but never lock the user-interface or render partial re-draws of the UI.  "Perceived Speed" from the user's point of view (meaning responsiveness) is King!

Testing:
	In addition to your normal testing, be sure to inform me if I have to recompile the .Net portion of the project or just Press Ctrl + F5 on the browser to reload to see the latest changes.  If making CSS / image changes, ensure that I will encounter no caching issue when testing in the browser.  Do not test the 3D About flyby unless you made changes to it.

Environment:
	You are in a dev machine.  You can drop and re-create the database as you see fit, especially when testing migrations scripts.  You don't need to preseve data in the backend or frontend.  You can also clear preferences, local storage settings and any cache as you see fit.

Database:
	If making changes to the database, that affects existing users see database-versioning.md under the docs folder.

Commits:
	SMILE is a public repo, write a detailed commit message so people can follow its progression.  Prefix all of your commits with "Sin and Codex: ".

Task Completion:
	As you complete each task and you are ready for me to test it, highlight them in bold and echo the task and tell me when it is ready for testing.  And when all of your tasks are completed, when there is nothing left in the prompt queue, re-display all the tasks that are ready for me to test in bold.

Tasks
-----
1.) When SMILE launches, default the window to its maximize state.
2.) As it is Building and Running, I want to see the Output window scroll.
3.) After the Build process is done, can you open the folder where the resulting code is for that (or all) languages.  If a folder was already opened, re-use the previous folder but just bring it to the forefront.
4.) Have an option (or a menu option) where #3 can be toggled on/off.
5.) We want the user to be able to inspect the generated code for the languages as a part of their learning.

6.) Can you add File menu option so that the user can save/load their SMILE code?  The file extension can be .smile.
7.) Put an About menu option also and update it with the correct build version of SMILE as we progress.
8.) Your build runs folder generate folders with names like this "20260802002305394-5b26ce70b98a4a8180b8246959341d26", can you also put a " - {language}" so the user has some sort of clue which programming language that folder is for?
9.) Let's make it a permanent rule that you clean up your build folder after 1 day.  Delete anything older than 1 day old.
10.) Is it possible to take a screenshot of SMILE as it is right now and make that be part of Day 1-2 progress and have that screenshot be featured in the README.md file?
11.) Let's add an option that is on by default so that the output generated by SMILE has a "Press any key to exit" and append that in the final .exe generated.  The problem is, right now, when I double click to run the program SMILE generates, it runs so fast it just opens and closes as if nothing happens.
12.) If possible, I would like more comments on the assembly generated code explaining what it does.  Typical of assembly programmers and have comments to the right-side of the code.
13.) Or, the simplest approach for #11, might have been to just generate a .bat file with the corresponding .exe so it's the batch file that can have a pause at the end?  So the user can see the output?  Would that work?

AI and Sin Chatting and Exploring Ideas
---------------------------------------
The AI actually agreed with me.  It had already started creating this complex pause algorithm for each language that it would insert at compile time, but a simple .cmd file (it went with that) in the end keeps the spirit of KISS alive.


14.) Can you download the correct JDK so you can compile java on this machine locally?  You have my permission.
15.) Can we also add support for transpilling to Objective-C and Swift?  Even if we are unable to compile it on a Windows VM machine.

16.) Follow and execute the instructions provided "C:\SMILE\Requirements\2026-08-02 1011 - ChatGPT Audit of Codex's Work.md".  These are corrections to some of the deficiencies in your last commit that ChatGPT noticed and should be corrected.

Formalizing the PRINT Statement with ChatGPT
--------------------------------------------
17.) Give me two mark down files for download:  1.) Formalizes the PRINT statement for the SMILE language so it can be published in the repo publicly as the official PRINT syntax for SMILE; 2.) Instructions for Codex to implement the PRINT statement as we discussed in our conversation.
18.) The 2nd markdown file is to have Codex implement the formal syntax of PRINT for SMILE. And have it generate tests cases so it can test variations of it to make sure it complies with the PRINT formal syntax.

To Codex
--------
19.) For background information, I have a the formal SMILE language declaration / specification for the PRINT statement.  It is located in "C:\SMILE\docs\SMILE Language Specification\SMILE - PRINT Statement Official Specification v1.0.md".
20.) I have the official Codex instructions provided by ChatGPT 5.6 for you to execute and implement the formal language PRINT statement for SMILE located in "C:\SMILE\Requirements\2026-08-02 1639 - SMILE PRINT Statement - Codex Implementation Instructions.md" read and follow its instructions.
21.) When you are done implementing it and all your tests are green, commit all and push to the repo.

Codex Completion Notes
----------------------
**19-21 ready for testing:** SMILE v0.2.0 Friendly PRINT implements the official PRINT forms, minimal string LET declarations, case-insensitive binding, raw and quoted interpolation, concatenation, deterministic PRINT Name versus PRINT {Name}, target-neutral binding, and generation across C#, C, MASM x64, JavaScript, Java, Objective-C, and Swift.

Validation completed locally: Debug build/tests, Release build/tests, CLI generation for all seven targets, CLI compile/run for C#, C, MASM x64, JavaScript, and Java, and desktop WPF smoke validation with maximized launch, screenshot refresh, 100-line paste, diagnostics/fix flow, duplicate PRINT diagnostic, Build & Run output, and preservation of build logs after live transpilation.

The Microsoft JDK installed on this VM is also detected from its Program Files folder when it is not on PATH, so Java Build & Run works locally without requiring a global PATH edit.

To Codex
--------
22.) I have added the official SMILE language specification for the LET statement located in "C:\SMILE\docs\SMILE Language Specification\SMILE - LET Statement Official Specification v1.0.md"
23.) I also moved the official SMILE language specification for the PRINT statement now located in "C:\SMILE\docs\SMILE Language Specification\SMILE - PRINT Statement Official Specification v1.0.md"
24.) Update any internal references you may have, and commit all and push to the repo.
25.) When I go to the Github repo of smile, I noticed the screenshot on the readme.md file is not getting updated to the latest one.  Can you update it based on what is stored in "C:\SMILE\Requirements\Progress\2026-08-02-day-1-2-smile-desktop.png"?  Thanks.

Codex Completion Notes
----------------------
**22-25 ready for testing:** The official PRINT and LET specifications now live under `C:\SMILE\docs\SMILE Language Specification`, README/AGENTS/language docs point to that canonical folder, the old duplicate PRINT specification locations were removed, and README now uses a fresh versioned screenshot file copied from `C:\SMILE\Requirements\Progress\2026-08-02-day-1-2-smile-desktop.png` so GitHub should display the v0.2.0 Friendly PRINT image.


To Codex
--------
26.) I noticed that while you translate the PRINT statement syntactically correct from SMILE to the target language, the intent is lost especially when interpolation is used.  I have researched the issue with ChatGPT 5.6 and you will find in "C:\SMILE\Requirements\2026-0-8-02 2001 - SMILE Codex Instructions Preserve PRINT Expression Intent.md" the correction to your logic.  Read the mentioned file and execute / implement it.

Codex Completion Notes
----------------------
**26 ready for testing:** SMILE now preserves PRINT expression intent in generated code. Friendly raw interpolation and explicit `$"..."` interpolation generate native interpolation in C#, JavaScript, and Swift; explicit `"..." + Name` concatenation remains concatenation; Java uses concatenation as the interpolation fallback; and C, Objective-C, and MASM still use equivalent segment output.

Validation completed locally: `dotnet restore SMILE.sln`, Debug build/tests, Release build/tests, CLI generation for all seven targets, and CLI compile/run for C#, C, MASM x64, JavaScript, and Java with identical normalized runtime output.


Sin's Review of Codex's Work So Far with ChatGPT
------------------------------------------------
Codex just committed the latest code in GitHub.com/Sincioco/smile .

Examine the SMILE source code below and the target languages (C# and C below).  The C# one looks close to the way I would write the code manually so don't make any changes there.  But the C version of the code does not look like the kind of code a normal C developer would write.  While syntactically correct, I'm sure it compiles and runs fine, it's not conventional.  Is it possible for SMILE to generate a "better looking" more "human natural" code that a typical human being / developer would write?  Again the C# version is fine, it's the C version of the code that I question.  I prefer a more idiomatic version (see below).

And would it be possible to make this a general rule across all the target languages? I would always prefer: Generated target code should be semantically correct, idiomatic for the destination language, and close to code a competent human developer would naturally write.

------------------------
SMILE Code
------------------------

LET Name = "Sin"

PRINT
PRINT "Hello World!"
PRINT Hello World!
PRINT Hello {Name}!
PRINT $"Hello {Name}!"
PRINT "Hello " + Name + "!"


------------------------
C# Code
------------------------

using System;

internal static class Program
{
    private static void Main()
    {
        string Name = "Sin";
        Console.WriteLine();
        Console.WriteLine("Hello World!");
        Console.WriteLine("Hello World!");
        Console.WriteLine($"Hello {Name}!");
        Console.WriteLine($"Hello {Name}!");
        Console.WriteLine("Hello " + Name + "!");
    }
}

------------------------
C Code - What SMILE Generates Right now
------------------------

#include <stdio.h>

int main(void)
{
    const char *Name = "Sin";
    putchar('\n');
    fputs("Hello World!", stdout);
    putchar('\n');
    fputs("Hello World!", stdout);
    putchar('\n');
    fputs("Hello ", stdout);
    fputs(Name, stdout);
    fputs("!", stdout);
    putchar('\n');
    fputs("Hello ", stdout);
    fputs(Name, stdout);
    fputs("!", stdout);
    putchar('\n');
    fputs("Hello ", stdout);
    fputs(Name, stdout);
    fputs("!", stdout);
    putchar('\n');
    return 0;
}



------------------------
C Code - Idiomatic Version (preferred version)
------------------------
#include <stdio.h>

int main(void)
{
    const char *name = "Sin";

    printf("\n");
    printf("Hello World!\n");
    printf("Hello World!\n");

    printf("Hello %s!\n", name);
    printf("Hello %s!\n", name);
    printf("Hello %s!\n", name);

    return 0;
}


To Codex
--------
27.) Remember this and make this a general rule for the project.  I would always prefer: Generated target code should be semantically correct, idiomatic for the destination language, and close to code a competent human developer would naturally write.

Codex Completion Notes
----------------------
**27 ready for testing:** SMILE now records this as a project-wide target generation rule. C and Objective-C PRINT generation uses one safe `printf` statement per SMILE `PRINT` where practical, while C#, JavaScript, Swift, Java, and MASM continue to preserve SMILE behavior in the clearest natural form available for each destination language. SMILE preserves user-authored identifier spelling in generated target code.

28.) Also the screenshot of the README.md file in the github repo of SMILE is still the original one....it still not updated.  Your last attempt to update it did not appear to have worked.