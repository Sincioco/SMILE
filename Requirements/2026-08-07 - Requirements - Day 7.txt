AI Instructions
--------------
Design and Coding Methodology:
	KISS (Keep It Simple Stupid) - The simpler the better.  Avoid over-engineering.  Avoid unnecessary complexity.  Avoid unnecessary features.  Avoid unnecessary bells and whistles.  Avoid unnecessary abstractions.  Avoid unnecessary patterns.  Avoid unnecessary frameworks.  Avoid unnecessary libraries.  Avoid unnecessary dependencies.  Avoid unnecessary code.  Avoid unnecessary files.  Avoid unnecessary folders.  Avoid unnecessary classes.  Avoid unnecessary methods.  Avoid unnecessary variables.

	KISS v2 (Keep It Speedy Sin) "The Sin Way" - Follow the original KISS method but with the mindset of creating a solution that gives the best possible performance both in terms of user-experience (first priority) and functional performance (second priority).  The user-experience may take precedence over the functional performance for any task.  For example, if perform a long-running task such as recalculating the positions of objects on screen (think Diagram 1), it should not lock the thread the user is on and give the impression that the application is stuck or sluggish.  The actual work, the functional part that does the calculations and moving objects on the screen, may be done slower, in chunks, or in a background / unobtrusive way, in order to give the user the best user-experience and responsiveness.  Never sacrifice performance for simplicity.  Speed is King!  But always design solutions and write code, that respect the user's experience and the application's responsiveness, over pure performance that often results in a negative user experience in the end.  If the two directions are in conflict, I will make it simple:  pick the user-experience first and just display a gauge to perform long running tasks in the background, but never lock the user-interface or render partial re-draws of the UI.  "Perceived Speed" from the user's point of view (meaning responsiveness) is King!

Testing:
	In addition to your normal testing, be sure to inform me if I have to recompile the .Net portion of the project or just Press Ctrl + F5 on the browser to reload to see the latest changes.  If making CSS / image changes, ensure that I will encounter no caching issue when testing in the browser.  Do not test the 3D About flyby unless you made changes to it.

Environment:
	You are in a dev machine.  You can drop and re-create the database as you see fit, especially when testing migrations scripts.  You don't need to preseve data in the backend or frontend.  You can also clear preferences, local storage settings and any cache as you see fit.

Database:
	If making changes to the database, that affects existing users see database-versioning.md under the docs folder.

Commits:
	SMILE is a public repo, write a detailed commit message so people can follow its progression.  Prefix all of your commits with "Sin and Codex: ".  "commit all", "commit everything", or just "commit" means all unstaged and staged changes in the repo.  "commit all and push" means commit all and push to the public repo but make sure your tests are green first before pushing to the public repo.

Task Completion:
	As you complete each task and you are ready for me to test it, highlight them in bold and echo the task and tell me when it is ready for testing.  And when all of your tasks are completed, when there is nothing left in the prompt queue, re-display all the tasks that are ready for me to test in bold.


SMILE Editor Hardening
----------------------
1.) Follow and execute the instructions in "D:\SMILE\Requirements\2026-08-07 1648 - SMILE Codex Instructions v0.7.0.1 Target-Editor Hardening.md".  Commit all and push to the public repo when done.

	Status: **Implemented and locally validated.** SMILE v0.7.0.1 treats each visible target pane as an independent editable build unit, including duplicate-language panes. Per-pane learner-edit revisions prevent older live generation from replacing newer target edits while still updating unaffected siblings, and later SMILE edits remain authoritative. Edited panes show `*` until authoritative regeneration, same-pane language switching, or New; Save Source, Build & Run, toolchain refresh, unrelated sibling changes, and Maximize/Restore preserve it. Build output identifies pane plus language and retains current INPUT metadata, project files, and companion files.

	Validation: restore completed successfully. Debug and Release builds each completed with 0 warnings and 0 errors. The final focused Desktop/highlighting suite passed 84/84 tests with 0 skipped. Final normal Debug, normal Release, strict Debug, and strict Release each passed 963/963 tests with 0 skipped. Strict runs required Java, all ten target toolchains, exact evaluator conformance, and zero detected generated compiler warnings.

	Manual Desktop smoke: first paint and cumulative language loading remained responsive; all target panes were editable; Save Source and successful build preserved `*`; two independently edited C# panes built and ran distinct programs in pane order with pane-plus-language headers; the edit-during-generation race, later-SMILE authority, sibling updates, language-switch ownership, Maximize/Restore, New, target-only build after New, and exact `0.7.0.1 Target Editor Hardening` About identity passed. INPUT generation selected the visible interactive-console policy, created exactly one press-any-key launcher and `cmd`/`conhost`/generated-program process chain, kept the IDE responsive, and cancelled cleanly. The Codex computer-control host presented that console headlessly even when SMILE was launched through File Explorer, so visual typing inside the separate console remains a user-side confirmation. Exact-SHA GitHub Actions verification remains the mandatory post-push completion gate.


WHILE Loops
-----------
2.) For reference, the WHILE language specification is at "D:\SMILE\docs\SMILE Language Specification\009 - SMILE-WHILE-Statement-Official-Specification-v1.0.md".
3.) Implement the WHILE statement in SMILE by following the instructions in "D:\SMILE\Requirements\2026-08-17 1929 - SMILE - Codex Instructions v0.8.0 WHILE Loops.md".  Commit all and push to the public repo when done.

	Status: **Implemented and locally validated.** SMILE v0.8.0 adds canonical `WHILE condition` / `END WHILE` loops across the lexer, parser, binder, fixed-point analysis, evaluator, and all ten target generators. Loops retain genuine target control flow, exact source comments and blank lines, mixed IF/WHILE nesting protection, checked runtime failures, cancellation, collision-safe target names, and zero-or-more-iteration analysis. The Desktop packages cumulative `language.smile` and focused `while.smile` examples, highlights WHILE without coloring strings or comments as keywords, preserves asynchronous revision-safe live transpilation, and reports the exact `0.8.0 WHILE Loops` identity. The canonical specification is `docs/SMILE Language Specification/009 - SMILE - WHILE Statement Official Specification v1.0.md`; README, architecture, roadmap, toolchain, generation-standard, earlier official specifications, and project instructions are synchronized with the implemented behavior.

	Validation: restore completed successfully. Debug and Release builds each completed with 0 warnings and 0 errors. Final normal Debug, normal Release, strict Debug, and strict Release each passed 1054/1054 tests with 0 skipped. Both strict runs required Java, all ten target toolchains, exact stdout/stderr/exit behavior, scripted stdin, the cumulative reference, and zero generated compiler warnings. The final strict focused WHILE suite passed 94/94 with 0 skipped; the all-target WHILE runtime class passed 11/11 with 0 skipped; and the final combined IF/WHILE parser conformance suite passed 192/192 in both Debug and Release. Coverage includes mixed 1,000-level recovery, accepted-outer-block terminator and ELSE ownership, fixed-point and String-growth analysis, runtime failures, cancellation, exact short-circuit behavior, and target-editor/highlighting regressions. A Release publish verification packaged both `language.smile` and `while.smile`.

	CLI smoke: `examples/while.smile` transpiled to all ten targets with exit 0. The normative acceptance program received scripted input `3` and produced exactly `Enter a positive count:`, `Count=3, Total=3`, `Count=2, Total=5`, `Count=1, Total=6`, and `Done. Total=6`, with empty stderr and exit 0. A captured JavaScript infinite WHILE terminated through the normal 10-second safety timeout with no stale generated source; the generated process reported exit -1 and the CLI returned exit 1 as required.

	Manual Desktop smoke: the real Release WPF app painted responsively, asynchronously loaded the cumulative reference, highlighted comments green and WHILE/END WHILE as blue keywords, and live-transpiled a finite loop with preserved comments and blank lines into genuine visible C#, MASM, and C control flow. Target edits gained `*`, built independently, and were replaced only by a later authoritative SMILE edit. Two duplicate C# panes retained independent edits and produced `Pane one` and `Pane two` under their own pane headers. Maximize/Restore preserved code and state, and New cleared SMILE plus every target editor. C#, MASM, COBOL, Python, and JavaScript loops each built and ran from their real pane with exact `Count=1`, `Count=2`, `Done=3` output and exit 0. An INPUT-driven JavaScript WHILE opened one visible console, displayed `Enter TRUE or FALSE:` before input, left the WPF UI responsive enough to Maximize while waiting, and Cancel closed the console/process tree and recorded `Cancelled.` About displayed the exact `0.8.0 WHILE Loops` identity; the unchanged 3D flyby was not exercised. Exact-SHA `SMILE CI` remains the mandatory post-push completion gate.

4.) Read and follow the instructions in "D:\SMILE\Requirements\2026-08-07 2352 - SMILE Codex Instructions - LET Block Strings and Idiomatic Multiline Strings v0.8.0 FULL.md".  Commit all and push when all your tests are green.